{
  "name": "Maven Conan PoC - AlmaLinux 9",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "dev",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "mvn --version && g++ --version && conan --version",
  "remoteUser": "developer"
}
