{
  "name": "Maven CMake PoC - AlmaLinux 9",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "dev",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "mvn --version && g++ --version && cmake --version",
  "remoteUser": "developer"
}